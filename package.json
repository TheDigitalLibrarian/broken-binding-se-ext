{
  "name": "broken-binding-se-ext",
  "version": "1.3.1",
  "description": "Browser enhancement for The Broken Binding. Currently rebuilds the account/order-history page — one merged list with totals, covers, currency conversion, a book grid, address management, and a private mode; an improved store-browsing view is planned.",
  "author": "João Morais",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build:min": "terser src/broken-binding-se-ext.js -c -m --toplevel -o dist/broken-binding-se-ext.min.js",
    "build:user": "python3 build/build_user.py",
    "build:page": "python3 build/build.py",
    "build": "npm run build:min && npm run build:user && npm run build:page",
    "package": "powershell -NoProfile -ExecutionPolicy Bypass -File build/package.ps1",
    "release": "npm run build && npm run package",
    "check": "node -e \"require('acorn').parse(require('fs').readFileSync('src/broken-binding-se-ext.js','utf8'),{ecmaVersion:5});console.log('src ES5 OK')\"",
    "test": "node test/run.mjs"
  },
  "devDependencies": {
    "acorn": "^8.11.0",
    "playwright": "^1.56.0",
    "terser": "^5.31.0"
  }
}
